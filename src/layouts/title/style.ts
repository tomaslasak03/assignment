import { styled } from '@config/theme';

export const TitleS = styled('div', {
  width: '100%',
  py: '$5',
  mb: '$5',
  h1: {
    fontSize: '$xlg',
    whiteSpace: 'pre-line',
    textAlign: 'center',
    textTransform: 'uppercase',
    fontWeight: 900,
  },
});
